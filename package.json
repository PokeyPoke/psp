{
  "name": "political-sentiment-tracker",
  "version": "1.0.0",
  "description": "A full-stack app for tracking political candidate sentiment across social media and news",
  "main": "backend/server.js",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "heroku-postbuild": "npm run install-backend && npm run install-frontend && npm run build-frontend && npm run copy-build",
    "install-backend": "cd backend && npm install",
    "install-frontend": "cd frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "copy-build": "mkdir -p backend/public && cp -r frontend/build/* backend/public/",
    "install-deps": "npm run install-backend && npm run install-frontend",
    "migrate": "cd backend && npm run migrate",
    "test": "cd backend && npm test && cd ../frontend && npm test"
  },
  "keywords": ["politics", "sentiment", "analytics", "react", "express"],
  "author": "Political Sentiment Tracker Team",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}